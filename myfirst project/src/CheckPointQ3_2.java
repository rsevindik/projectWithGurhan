public class CheckPointQ3_2 {

    public static void main(String[] args) {

        int x;
        int y = 20;

        if (y ==20) {
            x = 0;
            System.out.println("If y is: " + y + " then x is going to be;" + x);
        }

/**
Scanner keyboard = new Scanner (System.in);

        double totalHours;
        double weeklyHours = 40;
        double payRate;
        double extPayRate;

        System.out.println("Please enter your total worked hours in this week:");
        totalHours = keyboard.nextDouble();

            if (totalHours <= 40){
                payRate = 12.0;

            else (payRate = payRate * 1.5);
                System.out.println("Total earning this week: $ " + (weeklyHours * payRate) + ((totalHours - weeklyHours) * extPayRate));
            }
*/
    }
}
