package SelfPractices_Summer_B20;

import java.util.Arrays;

public class ArraysWithHasanA {
    public static void main(String[] args) {
/*  Hasan abinin dersi
        System.out.println(Arrays.toString(intArray)); // array method class (import java.util.Arrays;) added
        for (int eachscore : scores ) {
            eachscore += 5;
            System.out.println(eachscore);

        int [] scores = {80,90,65,75,95};
        // index 0   1  2  3  4

        Arrays.sort(scores);  // DONOT use unless for one shot CUZ index num degistiriyor
        System.out.println(Arrays.toString(scores));

        int max = scores[0];
        int min = scores[0];
        for (int i = 1; i <= scores.length-1 ; i++) {

            if(max <scores[i]){
                max = scores[i];

            }
            if(min > scores[i]){
                min = scores[i];
            }
        }
        System.out.println(max);
        System.out.println(min);

            int i=0;
        for ( i=0;i <= scores.length-1 ; i+=1) {
            if(max == scores[i]){
                System.out.println(i);
               break;
            }
            System.out.println(names[i-1]+" "+scores[i-1]);
        }

 */
    }
}
